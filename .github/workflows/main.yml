name: "Main"

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1

      - uses: ./
        with:
          token: ${{ secrets.DIAWI_TOKEN }}
          file: ./example.apk
